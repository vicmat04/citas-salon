# Tasks: update-readme

- [x] Reemplazar el contenido completo del archivo `README.md` en la raíz del proyecto con el bloque markdown definido en `openspec/changes/update-readme/design.md`.
- [x] Verificar que el formato del archivo `README.md` se renderiza correctamente (sin errores de sintaxis markdown).
